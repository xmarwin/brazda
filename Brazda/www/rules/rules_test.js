'use strict';

describe('myApp.rules module', function () {

    beforeEach(module('myApp.rules'));

    describe('rules controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view1Ctrl = $controller('RulesCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});