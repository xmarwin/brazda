'use strict';

describe('myApp.pravidla module', function () {

    beforeEach(module('myApp.pravidla'));

    describe('view1 controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view1Ctrl = $controller('PravidlaCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});