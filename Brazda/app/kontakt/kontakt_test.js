'use strict';

describe('myApp.kontakt module', function () {

    beforeEach(module('myApp.kontakt'));

    describe('kontakt controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view1Ctrl = $controller('KontaktCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});